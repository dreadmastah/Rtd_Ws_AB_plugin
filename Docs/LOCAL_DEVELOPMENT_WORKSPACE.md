# Local Development Workspace Reference

This document records the last known development-machine workspace used for
this repository.

It is informational only.

It MUST NOT be used as a runtime, build, test, deployment, or configuration
dependency.

## Repository

Remote repository:

`dreadmastah/Rtd_Ws_AB_plugin`

## Last Known Development Workspace

Development PC local repository:

`D:\wsrtd_binance_usdm_stack\autotrader`

Active WSRTD stack source:

`D:\wsrtd_binance_usdm_stack\autotrader\CleanRoomR2\stack`

## Last Verified Development Branch

`chatgpt/core-autotrader-scaffold-sim-only`

## Last Verified Runtime/Lifecycle Commit

`c76de000a3bfdb55e2743eb97c5a778b71a4ec5d`

This commit is the verified runtime/lifecycle baseline immediately before this
reference document was added.

## Portability Rule

All repository code and scripts should derive paths relative to the repository
or script location wherever possible.

The absolute paths documented above are historical/operator references only and
must not become required paths in source code, scripts, CI, tests, deployment,
or runtime configuration.

## Purpose

This file exists so that a future maintainer or development session can identify
the most recently used local workspace without making that workspace part of the
software architecture.
